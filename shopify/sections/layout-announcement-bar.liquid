{% if section.settings.visible %}
  {% if section.settings.home_page_only == false or template.name == 'index' %}
    <div class="container text-sm text-center py-2">
      {{ section.settings.content }}
    </div>
  {% endif %}
{% endif %}

{% schema %}
{
  "name":"t:sections.announcement-bar.name",
  "class": "bg-black text-white",
  "settings": [
    {
      "type": "checkbox",
      "id": "visible",
      "label": "t:sections.announcement-bar.settings.show-announcement-bar",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "home_page_only",
      "label": "t:sections.announcement-bar.settings.show-only-on-home-page",
      "default": true
    },
    {
      "type": "html",
      "id": "content",
      "label": "t:common.content",
      "default": "join our discord for updates, group buy info, and community discussions → <a href=\"#\" class=\"underline\">https://discord.gg/eJgCksvTYK</a>"
    }
  ]
}
{% endschema %}